# 03-进阶 / 03 - 工具调用与思考

> **TL;DR**：minimind-3 把 **Tool Call** 和 **Adaptive Thinking** 都下沉到 `chat_template` 层，而非模型架构层。同一个模型权重通过模板开关就能切换"会不会用工具""会不会显式思考"。本篇讲清数据格式、模板机制、评测脚本与 OpenAI API 调用方式。
> **心理学技巧**：**行为与环境**（Bandura 的交互决定论）—— 同一个"模型"在不同"模板环境"下表现出不同行为。理解这点后你会恍然大悟：所谓"思考模型"很多时候不是模型不同，而是提示模板不同。

## ✅ 你将能

- 写出 Tool Call 的对话格式（system 带 tools，assistant 带 tool_calls，tool 角色返回结果）
- 解释 chat_template 里 `tool_call` 标记的作用
- 用 `eval_toolcall.py` 测试模型的工具调用能力
- 解释 `open_thinking=0` 和 `open_thinking=1` 的差异
- 用 OpenAI 兼容 API 调用 minimind 的工具调用能力

---

## 步骤 0：先理解整体设计（5 分钟）

minimind-3 的关键设计决策（见 `README.md:820`）：

> 当前版本不再单独维护 `reason_*.pth` 权重，而是统一通过 `chat_template`、`think` 标签、`open_thinking` 开关以及后续 SFT / RLAIF 流程来建模"是否显式输出思考过程"。

也就是说：

| 能力 | 实现层 | 不在的层 |
|---|---|---|
| Tool Call | chat_template + SFT 数据 | 不在模型架构 |
| Adaptive Thinking | chat_template + thinking_ratio | 不在模型架构 |
| Self-Cognition | LoRA/SFT 数据 | 不在模型架构 |

**好处**：同一个 64M 模型，通过模板切换能当"普通助手""思考模型""工具调用 Agent"用，权重一份就够。

---

## 步骤 1：Tool Call 的数据格式

### 1.1 三种角色：system / assistant / tool

完整的 Tool Call 对话长这样（见 `dataset/lm_dataset.py:81-86` 的 `create_chat_prompt`）：

```jsonl
{"conversations": [
    {"role": "system", "content": "# Tools ...", "tools": "[{\"type\":\"function\",\"function\":{...}}]"},
    {"role": "user", "content": "现在几点？"},
    {"role": "assistant", "content": "", "tool_calls": "[{\"name\":\"get_current_time\",\"arguments\":{\"timezone\":\"Asia/Shanghai\"}}]"},
    {"role": "tool", "content": "{\"datetime\":\"2025-07-30 14:30:00\",\"timezone\":\"Asia/Shanghai\"}"},
    {"role": "assistant", "content": "现在是 2025-07-30 14:30。"}
]}
```

三个关键角色：

| 角色 | 作用 |
|---|---|
| `system` | 带 `tools` 字段，列出可用工具的 JSON Schema |
| `assistant` | 可带 `tool_calls` 字段，表示要调用哪个工具 |
| `tool` | 工具执行结果，content 是 JSON 字符串 |

### 1.2 chat_template 渲染后的样子

`model/tokenizer_config.json` 里的 chat_template 把上面渲染成（节选关键结构）：

```
im_start system
# Tools

You may call one or more functions to assist with the user query.
You are provided with function signatures within <tools>...</tools> XML tags:
<tools>
{"type": "function", "function": {"name": "get_current_time", ...}}
</tools>

For each function call, return a json object with function name and arguments
within tool_call XML tags:
  {"name": <function-name>, "arguments": <args-json-object>}

im_end
im_start user
现在几点？im_end
im_start assistant
  {"name": "get_current_time", "arguments": {"timezone": "Asia/Shanghai"}}

im_end
im_start user
tool
{"datetime": "2025-07-30 14:30:00", "timezone": "Asia/Shanghai"}

im_end
im_start assistant
现在是 2025-07-30 14:30。im_end
```

> 上图把 `im_start` / `im_end` 简写，真实是 `<|im_start|>` / `<|im_end|>` 这种 special token。完整模板见 `model/tokenizer_config.json` 的 `chat_template` 字段。

### 1.3 关键标记

| 标记 | 含义 | 在哪生成 |
|---|---|---|
| `<tools>...</tools>` | 包住工具 schema 列表 | system 段 |
| `tool_call` 开/闭标签 | 包住 assistant 的工具调用 JSON | assistant 段（含 tool_calls 时） |
| `<tool>...</tool>` | 包住工具执行结果 | tool 角色段（渲染为 user 角色） |

> 注意 chat_template 把 `tool` 角色的内容渲染为 `im_start user \n<tool>...<\/tool> im_end` 形式（见模板里 `message.role == "tool"` 分支），这是为了和模型训练时见过的格式对齐。

### 1.4 已混入 SFT 主线

`README.md` 提到 minimind-3 把 tool call 数据**混入 `sft_t2t` 主线**训练，所以 `full_sft` 权重本身就具备基础工具调用能力，不需要单独的 tool-call 训练阶段。

`dataset/lm_dataset.py:9-11` 也可看出：

```python
def pre_processing_chat(conversations, add_system_ratio=0.2):
    # tool use 数据完整保留不做处理
    if any(conv.get('tools') for conv in conversations): return conversations
```

Tool Call 数据不做随机加 system 等增强，保留原样保证格式正确。

---

## 步骤 2：用 eval_toolcall.py 测试

`scripts/eval_toolcall.py` 是内置的工具调用测试脚本，自带 8 个 mock 工具和测试用例。

### 2.1 本地模型测试

```bash
cd /path/to/minimind
python scripts/eval_toolcall.py \
  --backend local \
  --load_from ./model \
  --weight full_sft
```

启动后选 `[0] 自动测试`，会跑 8 个预设用例（`scripts/eval_toolcall.py:45-54`）：

```
[0] 自动测试
[1] 手动输入
```

每个用例显示：用户问题、可用工具列表、模型回复、工具调用过程、工具返回结果。完整一轮示例：

```
📦 可用工具: ['calculate_math', 'get_current_time']

💬: 帮我算一下 256 乘以 37 等于多少
🧠: [模型输出包含 tool_call 标签]
📞 [Tool Calling]: calculate_math | args={"expression": "256*37"}
✅ [Tool Called]: {"result": "9472"}
🧠: 256 乘以 37 等于 9472。
```

✅ **验证**：8 个用例中至少 4 个能正确调用工具并给出正确答案。`full_sft` 权重就具备这个能力，因为 SFT 数据已混入 tool call 样本。

### 2.2 关键代码解读

#### 2.2.1 工具解析（`scripts/eval_toolcall.py:70-78`）

```python
def parse_tool_calls(text):
    matches = re.findall(r'tool_call(.*?)tool_call_end', text, re.DOTALL)
    calls = []
    for m in matches:
        try:
            calls.append(json.loads(m.strip()))
        except Exception:
            pass
    return calls
```

> 上面的正则示意，真实正则见源码。核心是从模型输出里抽出 `tool_call` 标签内的 JSON，解析成 `{"name": ..., "arguments": ...}`。

#### 2.2.2 工具执行（`scripts/eval_toolcall.py:99-112`）

```python
def execute_tool(call, arguments=None):
    name = call.get("name", "") if isinstance(call, dict) else call
    raw_args = call.get("arguments", {}) if isinstance(call, dict) else arguments
    args = json.loads(raw_args) if isinstance(raw_args, str) else raw_args
    fn = MOCK_RESULTS.get(name)
    if not fn:
        return {"error": f"未知工具: {name}"}
    return fn(args)
```

mock 工具见 `eval_toolcall.py:29-38`，都是简单的 lambda 函数模拟真实 API 返回。

#### 2.2.3 多轮循环（`scripts/eval_toolcall.py:177-199`）

`run_case` 是核心循环：

```python
while True:
    content = generate(model, tokenizer, messages, tools, args)
    tool_calls = parse_tool_calls(content)
    if not tool_calls:
        break                                    # 模型直答，结束
    messages.append({"role": "assistant", "content": content})
    for tc in tool_calls:
        result = execute_tool(tc, ...)
        messages.append({"role": "tool", "content": json.dumps(result, ...)})
    # 继续下一轮，让模型基于工具结果生成最终答案
```

这就是 ReAct 范式：**思考 → 行动 → 观察 → 思考 → ... → 最终答案**。

### 2.3 用 transformers 格式权重测试

如果用 `convert_model.py` 转换后的 `minimind-3/`（transformers 格式）：

```bash
python scripts/eval_toolcall.py \
  --backend local \
  --load_from ./minimind-3
```

`eval_toolcall.py:57-67` 的 `init_model` 会根据 `--load_from` 自动判断用原生 torch 权重还是 transformers 加载。

### 2.4 手动输入测试

```bash
python scripts/eval_toolcall.py --backend local --load_from ./model --weight full_sft
# 选 [1] 手动输入
💬: 北京今天天气怎么样？
```

所有 8 个工具都可用（`eval_toolcall.py:18-27`）：calculate_math / get_current_time / random_number / text_length / unit_converter / get_current_weather / get_exchange_rate / translate_text。

---

## 步骤 3：Adaptive Thinking —— 模板层而非模型层

### 3.1 核心：open_thinking 开关

`model/tokenizer_config.json` 的 chat_template 末尾（`add_generation_prompt` 分支）：

```jinja
{%- if add_generation_prompt %}
    {{- 'im_start assistant\n' }}
    {%- if open_thinking is defined and open_thinking is true %}
        {{- 'think_start\n' }}                           ← 注入起始 think 标签
    {%- else %}
        {{- 'think_start\n\nthink_end\n\n' }}            ← 注入空 think 块
    {%- endif %}
{%- endif %}
```

**两种模式对比**：

| `open_thinking` | 注入内容 | 模型行为 |
|---|---|---|
| `0`（默认） | `think\n\nthink_end\n\n`（空思考块） | 模型直接给答案，不显式思考 |
| `1` | `think\n`（只起始标签） | 模型续写思考过程，到 `think_end` 后给答案 |

> 上表 `think` / `think_end` 简写，真实是 `` / `` 这对标签。

### 3.2 训练时的混合采样：thinking_ratio

模型要在两种模式上都表现好，训练时必须见过两种。`dataset/lm_dataset.py:195-216` 的 `RLAIFDataset`：

```python
class RLAIFDataset(Dataset):
    def __init__(self, ..., thinking_ratio=0.5):
        self.thinking_ratio = thinking_ratio
    def create_chat_prompt(self, conversations):
        use_thinking = random.random() < self.thinking_ratio
        return self.tokenizer.apply_chat_template(
            conversations[:-1],
            tokenize=False,
            open_thinking=use_thinking,        ← 50% 概率开思考
            add_generation_prompt=True
        )
```

PPO/GRPO 训练时通过 `--thinking_ratio 0.9` 控制（默认 90% 开思考）。Agentic RL 默认 `0.1`（10% 开思考，因为工具调用任务更看重动作而非显式思考）。

### 3.3 SFT 阶段的自适应技巧

`dataset/lm_dataset.py:31-35` 的 `post_processing_chat`：

```python
def post_processing_chat(prompt_content, empty_think_ratio=0.2):
    # 80% 概率移除空 think 标签
    if 'think\n\nthink_end\n\n' in prompt_content and random.random() > empty_think_ratio:
        prompt_content = prompt_content.replace('think\n\nthink_end\n\n', '')
    return prompt_content
```

> 这是 SFT 阶段的"80/20 技巧"：80% 移除空 think 标签（让模型见过"不思考直接答"），20% 保留（让模型见过"显式空思考块"）。这样训出的模型在两种模式下都不会崩。

### 3.4 推理时切换思考

```bash
# 不思考，直答
python eval_llm.py --load_from ./minimind-3 --open_thinking 0

# 显式思考
python eval_llm.py --load_from ./minimind-3 --open_thinking 1
```

✅ **验证**：用同一个问题"256×37=?"分别测，`--open_thinking 1` 会先输出一段思考（在 `think` 标签内），再给答案；`--open_thinking 0` 直接给答案。

> ⚠️ `--open_thinking 1` 要看到明显思考链需要**完整训练 + RLAIF** 的模型。mini 数据训出的 Zero 模型思考能力很弱（见 `docs/00-起步/03-跑通现成模型.md:155`）。

---

## 步骤 4：OpenAI 兼容 API 调用

### 4.1 启动 API 服务

```bash
cd scripts
python serve_openai_api.py --model minimind-3 --device cuda
```

默认监听 `http://localhost:8554/v1`。

### 4.2 用 OpenAI SDK 调用

`scripts/eval_toolcall.py:133-174` 的 `chat_api` 演示了完整调用：

```python
from openai import OpenAI

client = OpenAI(api_key="sk-123", base_url="http://localhost:8554/v1")

response = client.chat.completions.create(
    model="jingyaogong/minimind-3:latest",
    messages=[{"role": "user", "content": "现在几点？"}],
    tools=TOOLS,                                    ← 传工具列表
    stream=True,
    temperature=0.9,
    top_p=0.9,
)
```

用 `eval_toolcall.py` 测试 API 模式：

```bash
python scripts/eval_toolcall.py \
  --backend api \
  --api_base_url http://localhost:8554/v1 \
  --api_model jingyaogong/minimind-3:latest
```

✅ **验证**：选 `[0] 自动测试`，API 返回的 `tool_calls` 能被正确解析执行。

### 4.3 用 chat_template_kwargs 控制 open_thinking

OpenAI SDK 的 `extra_body` 可传模板参数：

```python
response = client.chat.completions.create(
    model="minimind-3",
    messages=[{"role": "user", "content": "256×37=?"}],
    extra_body={"chat_template_kwargs": {"open_thinking": True}},
)
```

`chat_template_kwargs` 会传给 `apply_chat_template` 的 kwargs，最终控制 chat_template 里的 `open_thinking` 变量。这是在不改模型权重的情况下动态切换"思考/直答"的标准做法。

> 也可用 `extra_body={"chat_template_kwargs": {"open_thinking": False}}` 强制直答模式。

### 4.4 流式解析 tool_calls

`eval_toolcall.py:148-170` 处理流式响应中的 tool_calls 拼接：

```python
for chunk in response:
    delta = chunk.choices[0].delta
    if delta.content:
        content += delta.content
    if delta.tool_calls:
        for tc_chunk in delta.tool_calls:
            idx = tc_chunk.index
            while len(tool_calls) <= idx:
                tool_calls.append({"id": "", "function": {"name": "", "arguments": ""}})
            if tc_chunk.function.name:
                tool_calls[idx]["function"]["name"] += tc_chunk.function.name
            if tc_chunk.function.arguments:
                tool_calls[idx]["function"]["arguments"] += tc_chunk.function.arguments
```

> tool_calls 是分块流式返回的，需要按 `index` 拼接。如果 API 返回的 content 里直接含 `tool_call` 标签（local backend 的格式），用 `parse_tool_call_from_text`（`eval_toolcall.py:81-96`）兜底解析。

---

## 步骤 5：训练自己的 Tool Call 能力

如果想增强模型的工具调用能力，有两个路径：

### 5.1 SFT 增强（最简单）

构造 tool call 数据（参考 `dataset/sft_t2t_mini.jsonl` 里的 tool call 样本），混入 SFT 数据训练：

```jsonl
{"conversations": [
    {"role": "system", "content": "# Tools", "tools": "[{\"name\":\"my_tool\",...}]"},
    {"role": "user", "content": "调用 my_tool"},
    {"role": "assistant", "content": "", "tool_calls": "[{\"name\":\"my_tool\",\"arguments\":{...}}]"},
    {"role": "tool", "content": "{\"result\":\"ok\"}"},
    {"role": "assistant", "content": "工具调用成功，结果是 ok。"}
]}
```

按 [02-实战/02-指令微调SFT](../02-实战/02-指令微调SFT.md) 训练即可。

### 5.2 Agentic RL 增强（更现代）

按 [03-进阶/02-DPO与强化学习](./02-DPO与强化学习.md) 的 Agentic RL 部分：

```bash
cd trainer
python train_agent.py \
  --from_weight full_sft \
  --data_path ../dataset/agent_rl.jsonl \
  --save_weight agent \
  --loss_type cispo
```

Agentic RL 会通过多轮 rollout + 工具执行 + 延迟奖励，让模型学会"何时该调工具、调哪个、怎么用结果"。

> `train_agent.py:40-47` 定义了 6 个 mock 工具，训练时用 `MOCK_RESULTS`（`train_agent.py:57-64`）模拟执行。想加自己的工具，改这两个字典即可。

---

## 🧯 踩坑提示

### Q1：`eval_toolcall.py` 报 "Can't load tokenizer"
路径错。`--load_from ./model` 是用 `model/` 下的代码 + `out/` 下的 `.pth`；`--load_from ./minimind-3` 是用 transformers 格式权重。检查 `ls minimind-3/tokenizer.json` 是否存在。

### Q2：模型不调工具，直接答
- 检查 `--weight` 是否为 `full_sft`（pretrain 阶段没学过工具）
- 检查 tools 参数是否真的传进去了（`eval_toolcall.py:117` 的 `tools=tools`）
- mini 数据训出的模型能力弱，换完整 SFT 数据训出的权重

### Q3：`--open_thinking 1` 没思考输出
mini 数据训出的 Zero 模型思考能力很弱。需要完整训练 + RLAIF 才能看到明显思考链（见 `docs/00-起步/03-跑通现成模型.md:155`）。

### Q4：API 模式 tool_calls 解析失败
`eval_toolcall.py:172-173` 有兜底：如果 `delta.tool_calls` 为空，用 `parse_tool_call_from_text` 从 content 里正则提取。检查 content 里是否有 `tool_call` 标签包裹的 JSON。

### Q5：SFT 时 tool call 数据 labels 全是 -100
`dataset/lm_dataset.py:88-104` 的 `generate_labels` 只在 `bos_id` 到 `eos_id` 之间算 loss。检查 tool call 数据的 `assistant` 段是否被正确渲染成 `im_start assistant ... im_end` 格式。

### Q6：Agentic RL 训练时模型不调工具
`train_agent.py:201` 检查 `tool_calls` 是否被 `parse_tool_calls` 正确解析。如果模型输出的 JSON 格式不对（缺引号、多余空格），会被 try/except 吞掉。开 `--debug_mode` 看原始输出。

### Q7：`extra_body` 传 `chat_template_kwargs` 不生效
部分 OpenAI 兼容服务器实现不全。minimind 自带的 `serve_openai_api.py` 支持，但第三方服务（如 vllm 旧版）可能需要单独配置。直接用 `scripts/chat_api.py` 测试。

---

## ✅ 本篇完成自检

<details>
<summary>点开自检（先想 30 秒）</summary>

1. Tool Call 数据有哪三种角色？分别带什么字段？
   - system 带 `tools`（工具 schema 列表）；assistant 可带 `tool_calls`（要调用的工具）；tool 角色返回 `content`（工具执行结果 JSON）。

2. chat_template 把 `tool` 角色渲染成什么？为什么？
   - 渲染成 `im_start user \n<tool>...<\/tool> im_end` 形式。为了让模型在训练时见过的格式一致，把 tool 角色伪装成 user 角色的一种特殊内容。

3. `open_thinking=0` 和 `open_thinking=1` 在 chat_template 里注入什么？差异是什么？
   - `=0` 注入 `think\n\nthink_end\n\n`（空思考块），模型直接答；`=1` 注入 `think\n`（只起始标签），模型续写思考过程到 `think_end` 再答。

4. `thinking_ratio=0.5` 在训练时控制什么？为什么需要混合？
   - 控制 50% 概率开思考注入。混合让模型见过两种模式，推理时无论用哪种都不会崩。如果只训思考模式，直答时会乱套；只训直答，思考能力退化。

5. SFT 阶段 `post_processing_chat` 的 80/20 技巧是什么？
   - 80% 概率移除渲染出的空 `think` 标签（让模型见过"无思考块直答"），20% 保留（让模型见过"显式空思考块"）。和 thinking_ratio 类似但作用在 SFT 数据上。

6. `eval_toolcall.py` 多轮循环的退出条件是什么？
   - `parse_tool_calls(content)` 返回空列表（模型直答，没有 tool_call 标签），就 break 退出。

7. OpenAI API 调用时 `extra_body={"chat_template_kwargs": {"open_thinking": True}}` 怎么生效？
   - SDK 把 extra_body 透传给服务器，服务器在调用 `apply_chat_template` 时把这些键作为 kwargs 传入，chat_template 的 jinja 里通过 `open_thinking` 变量取到，渲染时注入对应的 think 标签。

8. minimind-3 为什么说 Tool Call 和 Adaptive Thinking 都"不在模型架构层"？
   - 模型本体就是标准 Decoder-Only Transformer，没有任何专门处理工具调用或思考的模块。所有差异都在 chat_template 渲染出的不同输入文本上。同一个权重，换模板就是不同行为。

</details>

下一篇：回到 [README](../README.md) 或 [04-部署/01-推理服务与评测](../04-部署/01-推理服务与评测.md) 把你的模型部署起来。
