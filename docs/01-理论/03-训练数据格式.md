# 01-理论 / 03 - 训练数据格式

> **TL;DR**：minimind 有 5 种数据集类（Pretrain / SFT / DPO / RLAIF / AgentRL），
> 各对应不同训练阶段。这篇讲清每种格式 + chat_template 怎么套。

## ✅ 你将能

- 说出每种数据集的 jsonl 格式
- 解释 SFT 的 labels 为什么要 mask prompt 部分（-100）
- 知道 `apply_chat_template` 在每阶段的角色
- 自己构造一条新的训练样本

---

## 一、5 种数据集总览

`dataset/lm_dataset.py` 定义了 5 个 Dataset 类：

| 类 | 文件 | 用途 |
|---|---|---|
| `PretrainDataset` | `pretrain_t2t(_mini).jsonl` | 预训练（词语接龙） |
| `SFTDataset` | `sft_t2t(_mini).jsonl` | 指令微调（多轮对话 + Tool Call） |
| `DPODataset` | `dpo.jsonl` | DPO 偏好学习（chosen/rejected） |
| `RLAIFDataset` | `rlaif.jsonl` | PPO/GRPO/CISPO 的 prompt 池 |
| `AgentRLDataset` | `agent_rl(_math).jsonl` | Agentic RL 多轮工具调用 |

---

## 二、Pretrain 数据

### 文件格式（`pretrain_t2t_mini.jsonl`）

每行一个 JSON：
```jsonl
{"text": "如何才能摆脱拖延症？治愈拖延症并不容易..."}
{"text": "清晨的阳光透过窗帘洒进房间..."}
{"text": "Transformer 通过自注意力机制建模上下文关系..."}
```

只有 `text` 字段，**无对话结构**。

### Dataset 逻辑（`dataset/lm_dataset.py:37-55`）

```python
class PretrainDataset(Dataset):
    def __getitem__(self, index):
        sample = self.samples[index]
        # 切 token，留出 2 位给 bos 和 eos
        tokens = self.tokenizer(
            str(sample['text']), add_special_tokens=False,
            max_length=self.max_length - 2, truncation=True
        ).input_ids
        # 加上 bos/eos
        tokens = [self.tokenizer.bos_token_id] + tokens + [self.tokenizer.eos_token_id]
        # padding
        input_ids = tokens + [pad] * (max_length - len(tokens))
        labels = input_ids.clone()
        labels[input_ids == pad] = -100     # ← 不在 pad 上算 loss
        return input_ids, labels
```

**关键点**：
- 一条样本就是一个完整文档，bos 开头 eos 结尾
- labels = input_ids 偏移一位，模型学的是"已知前 i 个 token，预测第 i+1 个"
- padding 部分 labels = -100（cross_entropy 的 ignore_index），不算 loss

---

## 三、SFT 数据

### 文件格式（`sft_t2t_mini.jsonl`）

普通对话：
```jsonl
{"conversations": [
    {"role": "user", "content": "你好"},
    {"role": "assistant", "content": "你好！"}
]}
```

含 Tool Call：
```jsonl
{"conversations": [
    {"role": "system", "content": "# Tools ...", "tools": "[{\"name\":\"get_time\",...}]"},
    {"role": "user", "content": "现在几点？"},
    {"role": "assistant", "content": "", "tool_calls": "[{\"name\":\"get_time\",...}]"},
    {"role": "tool", "content": "{\"time\":\"17:00\"}"},
    {"role": "assistant", "content": "现在是17点。"}
]}
```

含 reasoning_content（思考链）：
```jsonl
{"conversations": [
    {"role": "user", "content": "256×37=?"},
    {"role": "assistant", "content": "256×37=9472", "reasoning_content": "256×37...分步思考..."}
]}
```

### Dataset 逻辑（`dataset/lm_dataset.py:58-119`）

核心两步：

**步骤 1：用 chat_template 渲染**

```python
def create_chat_prompt(self, conversations):
    messages = []
    for message in conversations:
        # 把 tools/tool_calls 的字符串解析回 dict
        if message.get("role") == "system" and message.get("tools"):
            tools = json.loads(...)
        if message.get("tool_calls") and isinstance(message["tool_calls"], str):
            message["tool_calls"] = json.loads(...)
        messages.append(message)
    return self.tokenizer.apply_chat_template(
        messages, tokenize=False, add_generation_prompt=False, tools=tools
    )
```

渲染出的字符串长这样（以普通对话为例）：
```
<|im_start|>user
你好<|im_end|>
<|im_start|>assistant
你好！<|im_end|>
```

**步骤 2：生成 labels（核心技巧）**

```python
def generate_labels(self, input_ids):
    labels = [-100] * len(input_ids)   # 默认全部不算 loss
    i = 0
    while i < len(input_ids):
        if input_ids[i:i+len(self.bos_id)] == self.bos_id:
            # 找到 assistant 开头
            start = i + len(self.bos_id)
            end = start
            while end < len(input_ids):
                if input_ids[end:end+len(self.eos_id)] == self.eos_id:
                    break
                end += 1
            # 只在 assistant 内容部分算 loss
            for j in range(start, min(end + len(self.eos_id), self.max_length)):
                labels[j] = input_ids[j]
            i = end + len(self.eos_id)
        else:
            i += 1
    return labels
```

**这是 SFT 与 Pretrain 的最大区别**：
- Pretrain：整段都算 loss（每段都是知识）
- SFT：只在 assistant 回复部分算 loss（user/system/tool 是输入，不该学）

**为什么是 -100？** 因为 PyTorch 的 `F.cross_entropy(..., ignore_index=-100)` 会忽略 label=-100 的位置。

### 数据增强技巧（lm_dataset.py:9-35）

```python
def pre_processing_chat(conversations, add_system_ratio=0.2):
    # tool use 不动
    if any(conv.get('tools') for conv in conversations): return conversations
    # 20% 概率加一个随机 system prompt
    if random.random() < 0.2:
        return [{'role': 'system', 'content': random.choice(SYSTEM_PROMPTS)}] + conversations
    return conversations

def post_processing_chat(prompt_content, empty_think_ratio=0.2):
    # 80% 概率移除空的 think 标签
    if '⟨think⟩\n\n⟨/think⟩\n\n' in prompt_content and random.random() > 0.2:
        prompt_content = prompt_content.replace('⟨think⟩\n\n⟨/think⟩\n\n', '')
    return prompt_content
```

> 这就是"自适应思考"的训练技巧：让模型在训练时见到 80% 不思考、20% 思考的样本，学会"该想时想，该直答时直答"。

---

## 四、DPO 数据

### 文件格式（`dpo.jsonl`）

```json
{
  "chosen": [
    {"content": "Q", "role": "user"},
    {"content": "good answer", "role": "assistant"}
  ],
  "rejected": [
    {"content": "Q", "role": "user"},
    {"content": "bad answer", "role": "assistant"}
  ]
}
```

同一问题，good vs bad，让模型学会"更喜欢 good"。

### Dataset 逻辑（`dataset/lm_dataset.py:122-192`）

- 对 chosen 和 rejected 分别 `apply_chat_template`
- 各自生成 `input_ids` + `loss_mask`（只在 assistant 部分计算 loss）
- 返回 `x_chosen, y_chosen, mask_chosen, x_rejected, y_rejected, mask_rejected`
- 训练时计算 chosen 和 rejected 的 logp 之差，最大化 chosen/rejected 的相对优势

---

## 五、RLAIF 数据

### 文件格式（`rlaif.jsonl`）

格式同 SFT，但**最后一个 assistant 留空**：
```jsonl
{"conversations": [
    {"role": "user", "content": "Q1"},
    {"role": "assistant", "content": "A1"},
    {"role": "user", "content": "Q2"}
    // ← 这里结束，让模型自己续写
]}
```

### Dataset 逻辑（`dataset/lm_dataset.py:195-224`）

```python
class RLAIFDataset(Dataset):
    def __init__(self, ..., thinking_ratio=0.5):
        ...
    def create_chat_prompt(self, conversations):
        use_thinking = random.random() < self.thinking_ratio
        return self.tokenizer.apply_chat_template(
            conversations[:-1],         # ← 去掉最后一个空 assistant
            tokenize=False,
            open_thinking=use_thinking,  # ← 50% 概率注入 think 标签
            add_generation_prompt=True   # ← 加 generation prompt
        )
    def __getitem__(self, index):
        return {'prompt': prompt, 'answer': ""}   # ← answer 留给 rollout 续写
```

**关键概念**：`rollout` —— 让当前策略模型对 prompt 续写，得到若干候选回复，再用奖励信号打分。详见 [03-进阶/02-DPO与强化学习](../03-进阶/02-DPO与强化学习.md)。

---

## 六、AgentRL 数据

### 文件格式（`agent_rl.jsonl`）

含 tools 的多轮对话，且**最后一个 assistant 留空让模型续写**，并附 `gt`（ground truth）做最终校验。

### Dataset 逻辑（`dataset/lm_dataset.py:226-252`）

```python
class AgentRLDataset(Dataset):
    def parse_conversations(self, conversations):
        ...
        return messages[:-1], tools   # 去掉最后一个空 assistant
    def __getitem__(self, index):
        return {'messages': messages, 'tools': tools, 'gt': sample['gt']}
```

`gt` 用于 Agentic RL 的**规则奖励**（如数学题最终答案校验），属于 RLVR（RL from Verifiable Rewards）思路。

---

## 七、动手验证

```bash
cd /path/to/minimind
python -c "
from transformers import AutoTokenizer
from datasets import load_dataset

tok = AutoTokenizer.from_pretrained('./model')
ds = load_dataset('json', data_files='./dataset/sft_t2t_mini.jsonl', split='train', streaming=True)
sample = next(iter(ds))
print('原始:', sample)
prompt = tok.apply_chat_template(
    sample['conversations'], tokenize=False, add_generation_prompt=False
)
print('渲染:')
print(prompt)
ids = tok(prompt).input_ids
print('IDs:', ids[:20], '...')
print('总长:', len(ids), 'tokens')
"
```

✅ **验证**：能看到渲染出的对话字符串 + token id 列表。

---

## 八、自己造一条 SFT 数据

举例：让 minimind 知道你的名字。

```jsonl
{"conversations": [
    {"role": "user", "content": "你叫什么名字？"},
    {"role": "assistant", "content": "我叫 TJK，是一个由学习者自己训练的 minimind 模型。"}
]}
{"conversations": [
    {"role": "user", "content": "你是谁？"},
    {"role": "assistant", "content": "我是 TJK 训练的 minimind，主要用于学习 LLM 原理。"}
]}
```

保存为 `dataset/lora_identity.jsonl`，在 [03-进阶/01-LoRA微调](../03-进阶/01-LoRA微调.md) 里会用上。

## 九、关键代码对照

| 概念 | 代码位置 |
|---|---|
| Pretrain 切分 + bos/eos | `dataset/lm_dataset.py:47-55` |
| SFT chat_template 套用 | `dataset/lm_dataset.py:71-86` |
| SFT labels 只在 assistant 算 loss | `dataset/lm_dataset.py:88-104` |
| 概率性加 system prompt | `dataset/lm_dataset.py:9-29` |
| 80% 移除空 think | `dataset/lm_dataset.py:31-35` |
| DPO 双 prompt + loss_mask | `dataset/lm_dataset.py:122-192` |
| RLAIF 留空 assistant + thinking_ratio | `dataset/lm_dataset.py:195-224` |
| AgentRL 多轮 + gt | `dataset/lm_dataset.py:226-252` |

## ✅ 本篇完成自检

<details>
<summary>点开自检（先想 30 秒）</summary>

1. Pretrain 和 SFT 在 Dataset 层的最大区别？
   - Pretrain：整个序列都算 loss。SFT：只在 assistant 回复部分算 loss，labels 在 prompt 部分 = -100。
2. 为什么 padding 位置 labels = -100？
   - 让 cross_entropy 用 ignore_index=-100 忽略，不在 pad 上算 loss。
3. `pre_processing_chat` 20% 加 system prompt 的作用？
   - 数据增强，让模型见过 system 也见过没有 system 的情况，提高鲁棒性。
4. DPO 数据为什么成对（chosen/rejected）？
   - DPO 不显式建模奖励，而是直接最大化 chosen/rejected 的对数概率之差，让模型"更喜欢 good"。
5. RLAIF 数据为什么最后一个 assistant 要留空？
   - 给策略模型 rollout 用：让它自己续写，再用奖励信号打分。
6. `thinking_ratio=0.5` 控制什么？
   - RLAIF prompt 中 50% 概率注入 `` 标签，让模型见过"开思考"和"不开思考"两种状态。
7. AgentRL 的 `gt` 字段做什么用？
   - 提供最终答案的 ground truth，用于 RLVR（基于可验证奖励的强化学习），比如数学题最终答案校验。

</details>

下一篇：[02-实战/01-预训练Pretrain](../02-实战/01-预训练Pretrain.md) —— 真正开始训练！
